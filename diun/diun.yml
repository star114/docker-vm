watch:
  workers: 10
  schedule: "0 */6 * * *"
  firstCheckNotif: false

providers:
  docker:
    watchByDefault: false

notif:
  telegram:
    disableNotification: false
    templateBody: |
      *Docker image update*
      *Image:* {{ if .Entry.Image.HubLink }}[{{ .Entry.Image }}]({{ .Entry.Image.HubLink }}){{ else }}{{ .Entry.Image }}{{ end }}
      *Signal:* The configured image tag digest changed
      *Source:* {{ escapeMarkdown .Entry.Provider }}
      *Host:* {{ escapeMarkdown .Meta.Hostname }}
      Pull and recreate the stack when ready.
